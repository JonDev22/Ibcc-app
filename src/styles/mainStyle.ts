import { StyleSheet } from 'react-native';

export const mainStyles = StyleSheet.create({
    circleIcon: {
        borderRadius: 100,
        fontSize: 20,
        width: 40,
        height: 40,
        textAlign: 'center',
        lineHeight: 40,
    },
});
